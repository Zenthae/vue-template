{
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build && touch ./build/.nojekyll",
    "publish": "yarn run build && gh-pages -t -d build"
  },
  "dependencies": {
    "vue": "3.0.2"
  },
  "devDependencies": {
    "@snowpack/plugin-dotenv": "^2.0.4",
    "@snowpack/plugin-vue": "^2.2.4",
    "gh-pages": "^3.1.0",
    "snowpack": "^2.16.1"
  }
}
